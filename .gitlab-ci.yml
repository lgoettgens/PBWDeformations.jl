stages:
  - deploy

default:
  image: johannesflake/oscar:latest

pages:
  stage: deploy
  image: alpine:latest
  script:
  - echo 'Nothing to do...'
  artifacts:
    paths:
      - public
    expire_in: 1 day
  rules:
    - if: $CI_COMMIT_REF_NAME == "pages"
